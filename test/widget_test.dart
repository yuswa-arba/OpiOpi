import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('OpiOpi smoke test', (WidgetTester tester) async {
    // Placeholder test — integration tests require a live Google Sheet.
    expect(true, isTrue);
  });
}
